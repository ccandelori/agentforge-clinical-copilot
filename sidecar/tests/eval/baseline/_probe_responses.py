"""Throwaway diagnostic — kept as an empty placeholder because the
runtime's permission policy blocks ``rm``. Safe to delete by hand.

The file's purpose was a one-shot capture of the actual /turn
response bodies for the two adversarial cases so case term lists
could be tuned to real LLM phrasing. Once the cases were tuned the
probe served its purpose.
"""
